---
title: "Nesting Nooks: Exploring Squirrel Habitat"
---
tables and whatever about "where the squirrels live"

```{r setup, include=FALSE}
knitr::opts_chunk$set(collapse = TRUE, message = FALSE)
```

```{r}
library(tidyverse)
library(plotly)
```

#### Here is a map of squirrel location based on primary fur color. 

```{r}
squirrel_census = 
  read_csv("data/clean_squirrel_2018.csv") |> 
  janitor::clean_names()
```

```{r fur_plot}
squirrel_fur_graph =
  squirrel_census |> 
  select(unique_squirrel_id, x, y, primary_fur_color) |>
  filter(!is.na(primary_fur_color))

squirrel_fur_graph |> 
  plot_ly(x = ~x, y = ~y, type = "scatter", mode = "markers", color = ~primary_fur_color)
```

